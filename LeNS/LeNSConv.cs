using System;
using System.Collections.Generic;
using System.Text;

namespace LeNS
{
    // LeNSコンバート処理本体
    class LeNSConv
    {
        public void run(LeNSConvOption option)
        {
            /*
            String path;

            // コンバート対象で処理ファイル分岐
            switch (Option.conv)
            {
                case LeNSConvMode.MODE_SZ:
                    path = Option.pakPath + "\\MAX_DATA.PAK";
                    break;
                case LeNSConvMode.MODE_KZ:
                    path = Option.pakPath + "\\MAX2DATA.PAK";
                    break;
                case LeNSConvMode.MODE_TH:
                    path = Option.pakPath + "\\LVNS3SCN.PAK";
                    //path = Option.pakPath + "\\LVNS3DAT.PAK";
                    break;
            }

            // LeafPackオープンテスト
            try
            {
                pak.open(path, System.IO.FileMode.Open);

                MessageBox.Show("うはｗｗｗ　おｋｗｗｗｗｗ");
            }
            catch (Exception ex)
            {
                MessageBox.Show(ex.ToString(), "Exception", MessageBoxButtons.OK, MessageBoxIcon.Error);
            }
            finally
            {
                pak.close();
            }
            */
        }
    }
}
